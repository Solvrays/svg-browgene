playwright==1.58.0
# [video] extra pulls in the optional deps browser-use needs to encode the
# session MP4 recording (BrowserProfile.record_video_dir). Without it,
# recording silently fails with "MP4 recording requires optional dependencies".
browser-use[video]==0.12.0
pyperclip==1.9.0
gradio>=6.5.0,<7
json-repair
uvicorn==0.41.0
fastapi==0.134.0
pydantic>=2.12.0
